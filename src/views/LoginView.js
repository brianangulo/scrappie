import React from 'react';
import { View } from 'react-native';
import { Text, Button } from "react-native-elements";

function Login() {
    return (
        <View>
            <Button style={{justifyContent: "center", marginHorizontal: "30%"}} title="Login" />
        </View>
    );
}

export default Login;